# Deprecated as a separate install path.
# MuseTalk deps (diffusers, transformers, librosa, soundfile, accelerate,
# huggingface-hub) are now in requirements_cu13.txt and installed by the
# launcher ("Check / Update Dependencies").
#
# Kept for reference / older docs only. Prefer:
#   uv pip install -r requirements_cu13.txt
# or the portable launcher dependency update.

-r requirements_cu13.txt
